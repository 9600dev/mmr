#!/usr/bin/expect

set timeout 120
set MMR_DIR [lindex $argv 0];

spawn sh -c {
    DISPLAY= $MMR_DIR/tws-latest-standalone-linux-x64.sh
}

expect "Where should"
send "\r"

expect "Run Trader"
send "n\r"
