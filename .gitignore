*.pyc
__pycache__/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ipynb_checkpoints/
.vscode/
.devcontainer/

*.csv
*.tar*

scratch/
.python-version
.trader.history
.env
# Timestamped backups docker.sh takes before regenerating .env — they contain the
# IB password in plaintext, so they must never be committable. Deliberately NOT
# `.env.*`, which would cover the tracked .env.example.
.env.bak_*
.claude/
.venv/

# Runtime directories
/data/
/logs/
*.duckdb
*.duckdb.wal

# Local config overrides
config_defaults/local_*

notebooks/
.hypothesis/

# mutmut mutation-testing sandbox (scripts/run_mutation.sh) — a regenerable copy
# of the source tree with mutants applied; never committed.
mutants/
.mutmut-cache

# Operational state: live positions, stop levels, account state, and the
# strategy roster (symbols/conIds/PFs/tuned params). Untracked deliberately —
# 9600dev/mmr is PUBLIC and this file is the book's alpha plus its open risk.
# Kept on disk as the working reference; see docs/OPERATIONAL_STATE.README.md.
docs/OPERATIONAL_STATE.md
